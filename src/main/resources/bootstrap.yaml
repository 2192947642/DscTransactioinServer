spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    url: jdbc:mysql://106.3.99.64:20021/tjpu22t02
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: tjpu22t02
    password: tjpu22t02
    type: com.alibaba.druid.pool.DruidDataSource
  main:
    web-application-type: servlet
  application:
    name: TractSqlServiceServlet
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  web:
    resources:
      static-locations: classpath:/static,classpath:/resources

mybatis:
  configuration:
    auto-mapping-behavior: full
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  type-aliases-package: com.types.pojo
  mapper-locations: classpath:/mapper/*.xml
transact:
  netty:
    ip: 127.0.0.1 #注册到服务器的netty服务端ip
    port: 8036 # netty服务端端口